<aside class="sidebar">
  <p class="brand-kicker">{{sectionLabel}}</p>
  <h2 class="brand-title">{{systemName}}</h2>
  <p class="sidebar-meta">
    Unified payroll operations, compliance review, and audit visibility inside a
    single internal workspace.
  </p>

  <nav class="nav-group" aria-label="Primary">
    {{#each navItems}}
      <a class="nav-link" href="{{this.href}}">{{this.label}}</a>
    {{/each}}
  </nav>
</aside>
